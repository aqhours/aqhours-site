deploy.aqhours.cn {
	@deploy_hooks {
		method POST
		path /hooks/aqhours-site /hooks/eternal-card
	}

	handle @deploy_hooks {
		# aqhours-web's host-side gateway; the receiver binds only here.
		reverse_proxy 172.18.0.1:9000
	}

	respond 404
}
